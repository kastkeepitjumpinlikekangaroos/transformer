__pycache__
# Bazel build outputs
bazel-*

# Bazel lock file
MODULE.bazel.lock

# Bazel convenience symlinks
/bazel-bin
/bazel-out
/bazel-testlogs
/bazel-transformer

# Local cache for downloaded cloud objects
.transformer-cache/

# Validation result dumps from default location
validation_results/

# IDE / OS noise
.idea/
.vscode/
.DS_Store

examples/directory_app/job/output

# Dev-local macro-bench baselines (e.g. polymarket numbers, which depend on
# the Kaggle dataset checkout at ~/Downloads/archive/). The checked-in
# baselines under benchmarks/baseline/ are the spill-off / spill-on jaffle
# baselines + the polymarket.json.example template; everything else stays
# local to the developer.
benchmarks/baseline/local.*.json
